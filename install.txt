rm -rf node_modules .next .turbo package-lock.json pnpm-lock.yaml yarn.lock
npm cache verify
npm install

*********
Os erros se devem porque o SQLite não aceita enums:

Enums antogos:
enum Role {
  ATHLETE
  GUARDIAN
  AGENT
  PRESS
  FAN
  ADMIN
}

enum CopyrightStatus {
  OK
  POSSIBLE_INFRINGEMENT
  BLOCKED
}


## Atualizando o node para a versão mais recente:
nvm ls-remote
nvm install node
nvm use node
nvm alias default node

rm -rf node_modules .next .turbo package-lock.json pnpm-lock.yaml yarn.lock
npm cache verify
npm install

